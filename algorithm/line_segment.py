from algorithm import lexicographic_sort
from predicate import orient_2d
import numpy as np

def line_segment_intersection(segments):
    """
        Returns all intersection points of the segments.
    """
    return naive(segments)
def naive(segments):
